<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_bookmarks"
            android:icon="@drawable/ic_bookmark_filled"
            android:title="@string/characters_favorite"/>
        <item
            android:id="@+id/nav_characters_all"
            android:icon="@drawable/ic_character_all"
            android:checked="true"
            android:title="@string/characters_all"/>
        <item
            android:id="@+id/nav_characters_seen"
            android:icon="@drawable/ic_history"
            android:title="@string/characters_seen"/>
    </group>

</menu>
